# TarefoAI - Arquivo de exemplo de variáveis de ambiente
# Renomeie para .env e preencha com suas credenciais reais

# Banco de Dados
DATABASE_URL=postgres://user:password@localhost:5432/tarefo_ai
PGHOST=localhost
PGPORT=5432
PGDATABASE=tarefo_ai
PGUSER=postgres
PGPASSWORD=senha

# Configurações de Modelos de Linguagem (LLM)
# Você precisa definir pelo menos um destes para que o serviço de chat funcione

# OpenAI API (Padrão)
OPENAI_API_KEY=sk-seu-token-openai

# Anthropic API (Alternativa 1)
# ANTHROPIC_API_KEY=sk-seu-token-anthropic

# xAI/Grok API (Alternativa 2)
# XAI_API_KEY=seu-token-xai

# LLM Local (Alternativa 3 - para modelos executados localmente)
# LOCAL_LLM_URL=http://localhost:11434/v1
# LOCAL_LLM_KEY=não-necessário-para-muitos-servidores-locais
# LOCAL_LLM_MODEL=Qwen3-32B-Q3_K_L.gguf

# Integração com Telegram
TELEGRAM_BOT_TOKEN=seu-token-telegram-bot

# Integração com WhatsApp (Twilio)
TWILIO_ACCOUNT_SID=seu-account-sid-twilio
TWILIO_AUTH_TOKEN=seu-auth-token-twilio
TWILIO_PHONE_NUMBER=+5511999999999

# Configurações do Servidor
PORT=5000
NODE_ENV=development
SESSION_SECRET=change-this-to-a-secure-random-string

# Configurações do CrewAI
CREWAI_MODEL=gpt-4o
CREWAI_TEMPERATURE=0.2
CREWAI_VERBOSE=true